let username = prompt("User: ");
let password = prompt("Password: ");

if (password === "1234" && username === "1234") {
    alert("Logado com Sucesso!");
} else {
    alert("Username ou senha inválidos!");
}